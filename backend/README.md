tech stack: hono(http library)
            TS(main language)
            cloudeflare wrokers(cdn)
            prisma(orm)/connection pooling 
            postgress
            zod & jwt auth
            react.js
            docker
